name: pullrequest from issue

on:
  issues:

jobs:
  display:
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v1
        with:
          submodules: true
      - name: display
        run: |
          echo {{github.event}}
